---
title: codexclaw
description: "Codex-native development discipline: PABCD workflow, dev skills, subagent config, and an optional opencodex bridge, shipped as one Codex plugin."
template: splash
head:
  - tag: title
    content: "codexclaw — development discipline for OpenAI Codex"
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@graph": [
          {
            "@type": "SoftwareApplication",
            "name": "codexclaw",
            "applicationCategory": "DeveloperApplication",
            "operatingSystem": "macOS, Linux",
            "description": "Codex-native development discipline: PABCD workflow, dev skills, multi-model subagent config, and an optional opencodex bridge, shipped as one OpenAI Codex plugin.",
            "url": "https://lidge-jun.github.io/codexclaw/",
            "image": "https://lidge-jun.github.io/codexclaw/og.png",
            "license": "https://opensource.org/license/mit",
            "softwareVersion": "0.1.1",
            "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
            "author": { "@type": "Person", "name": "lidge-jun", "url": "https://github.com/lidge-jun" }
          },
          {
            "@type": "WebSite",
            "name": "codexclaw",
            "url": "https://lidge-jun.github.io/codexclaw/"
          }
        ]
      }
---

<section class="cxcp-poster" aria-labelledby="cxcp-title">
  <div class="cxcp-hero">
    <div class="cxcp-wedge" aria-hidden="true"></div>
    <div class="cxcp-construct" aria-hidden="true">
      <span class="cxcp-sphere"></span>
      <span class="cxcp-bar cxcp-bar--tall"></span>
      <span class="cxcp-bar cxcp-bar--wide"></span>
      <span class="cxcp-bar cxcp-bar--short"></span>
      <span class="cxcp-baseline"></span>
    </div>
    <div class="cxcp-message">
      <h1 id="cxcp-title">codexclaw</h1>
      <p class="cxcp-ribbon">Development discipline for OpenAI Codex</p>
    </div>
    <div class="cxcp-actions">
      <a class="cxcp-start" href="/codexclaw/getting-started/installation/">Get Started</a>
      <a class="cxcp-secondary" href="/codexclaw/concepts/how-it-works/">How It Works</a>
    </div>
  </div>

  <nav class="cxcp-panels" aria-label="Featured guides">
    <a class="cxcp-panel cxcp-panel--pabcd" href="/codexclaw/guides/pabcd/">
      <span class="cxcp-panel__label">PABCD</span>
    </a>
    <a class="cxcp-panel cxcp-panel--skills" href="/codexclaw/guides/skills/">
      <span class="cxcp-panel__label">Skills</span>
    </a>
    <a class="cxcp-panel cxcp-panel--subagents" href="/codexclaw/guides/subagents/">
      <span class="cxcp-panel__label">Subagents</span>
    </a>
  </nav>

  <div class="cxcp-footer-rule" aria-hidden="true"></div>
</section>

Workflow inspiration: [OMO / oh-my-openagent](https://github.com/code-yeongyu/oh-my-openagent).
Some components are adapted from historical MIT-licensed LazyCodex/OMO; see
[credits, pinned sources and modifications](https://github.com/lidge-jun/codexclaw/blob/dev/NOTICE.md).
CodexClaw is a separate, unofficial project.

<div class="cxc-term" role="img" aria-label="Terminal session showing a codexclaw PABCD work loop">
  <div class="cxc-term__bar" aria-hidden="true">
    <span class="cxc-term__dot"></span><span class="cxc-term__dot"></span><span class="cxc-term__dot"></span>
    <span class="cxc-term__title">cxc — PABCD loop</span>
  </div>
  <div class="cxc-term__body" aria-hidden="true">
    <span class="cxc-term__line"><span class="cxc-t-dim">$</span> codex plugin add codexclaw@codexclaw</span>
    <span class="cxc-term__line"><span class="cxc-t-ok">Added</span> plugin codexclaw</span>
    <span class="cxc-term__line cxc-term__gap"></span>
    <span class="cxc-term__line"><span class="cxc-t-dim">$</span> cxc orchestrate P --attest <span class="cxc-t-str">'&#123;"from":"IDLE","to":"P",…&#125;'</span></span>
    <span class="cxc-term__line">orchestrate P: IDLE <span class="cxc-t-red">-&gt;</span> P</span>
    <span class="cxc-term__line cxc-term__gap"></span>
    <span class="cxc-term__line"><span class="cxc-t-dim">$</span> cxc orchestrate status</span>
    <span class="cxc-term__line"><span class="cxc-t-red">IPABCD:</span> P <span class="cxc-t-dim">(PLAN)</span> · interview:false audit:false check:false</span>
    <span class="cxc-term__line cxc-term__gap"></span>
    <span class="cxc-term__line"><span class="cxc-t-dim">$</span> cxc map src/</span>
    <span class="cxc-term__line"><span class="cxc-t-ok">ranked</span> 42 files · tree-sitter + PageRank</span>
    <span class="cxc-term__line cxc-term__gap"></span>
    <span class="cxc-term__line"><span class="cxc-t-dim">$</span> cxc loop validate --slug release</span>
    <span class="cxc-term__line"><span class="cxc-t-ok">OK</span> — complete + all met criteria carry evidence</span>
  </div>
</div>

_The terminal above uses the `cxc` CLI. Every install has one: the plugin payload ships a
dispatcher (`node "<pluginRoot>/bin/cxc.mjs" …` — the SessionStart banner names the exact
invocation), and a repo checkout can put `cxc` on `PATH`. See
[Installation](/codexclaw/getting-started/installation/)._

**codexclaw** (`cxc`) is a single [OpenAI Codex](https://openai.com/codex) plugin. It reuses the
Codex runtime and adds development discipline on top: a `cxc-dev` work classifier, an IPABCD
work loop driven by hooks and file state, multi-model subagent configuration over MCP, a shipped
GUI dashboard (built from the repo checkout until the bundled dashboard ships), and an optional
detect-only [opencodex](https://github.com/lidge-jun/opencodex) provider bridge.

It is not a Codex fork, not a harness, and not a provider proxy. Everything ships through the
standard Codex plugin surfaces: skills, hooks, an MCP server, and local components. The `cxc`
CLI has two tiers for v0.1.1: the marketplace payload ships a dispatcher (`bin/cxc.mjs`) so
every install has a terminal surface, and a repository checkout can additionally place `cxc` on
`PATH` (npm link or alias). The only
scoped server exception is the opt-in loopback messenger bridge, which relays to
stock `codex exec` and does not orchestrate subagents.

<section class="cxc-sec">

## PABCD pipeline

<div class="cxc-rail" aria-label="codexclaw IPABCD pipeline">
  <div class="cxc-rail__step"><span class="cxc-rail__key">I</span><span class="cxc-rail__name">Interview</span><span class="cxc-rail__desc">settle ambiguity before a work loop starts</span></div>
  <div class="cxc-rail__step"><span class="cxc-rail__key">P</span><span class="cxc-rail__name">Plan</span><span class="cxc-rail__desc">write the concrete change plan and evidence target</span></div>
  <div class="cxc-rail__step"><span class="cxc-rail__key">A</span><span class="cxc-rail__name">Audit</span><span class="cxc-rail__desc">adversarial review against the real codebase</span></div>
  <div class="cxc-rail__step"><span class="cxc-rail__key">B</span><span class="cxc-rail__name">Build</span><span class="cxc-rail__desc">scoped atomic changes, verified as you go</span></div>
  <div class="cxc-rail__step"><span class="cxc-rail__key">C</span><span class="cxc-rail__name">Check</span><span class="cxc-rail__desc">real build, tests, and review gate</span></div>
  <div class="cxc-rail__step"><span class="cxc-rail__key">D</span><span class="cxc-rail__name">Done</span><span class="cxc-rail__desc">evidence summary, close to idle</span></div>
</div>

```
Codex runtime
  └─ codexclaw plugin
       ├─ skills   →  cxc-dev discipline + dev-* routers + PABCD/loop/interview
       ├─ hooks    →  PABCD state, goal guards, provider detection
       ├─ MCP      →  subagent model/prompt config
       ├─ GUI      →  dashboard (build from source) + opt-in messenger bridge
       └─ cxc CLI  →  ships in payload (bin/cxc.mjs); PATH cxc via checkout
```

</section>

<section class="cxc-sec">

## Surface map

| Surface | Status | Count / scope | Primary docs |
|---------|--------|---------------|--------------|
| Skills | <span class="cxc-badge cxc-badge--current">current</span> | 28 skills: the `dev` classifier parent plus 12 surface routers, `pabcd`, `loop`, `interview`, `search`, `recall`, `qa`, `repo-map`, and research/search helpers. | [Skills](/codexclaw/guides/skills/) |
| Hooks | <span class="cxc-badge cxc-badge--current">current</span> | 28 registered files, 29 handlers: `SessionStart` x8, `UserPromptSubmit` x5, `PreToolUse` x7, `PostToolUse` x2, `Stop` x2, `SubagentStop` x2, `PostCompact` x3. | [Hooks](/codexclaw/reference/hooks/) |
| MCP | <span class="cxc-badge cxc-badge--current">current</span> | Subagent model and prompt config: `subagents_get`, `subagents_set`, `catalog_list`. | [MCP Tools](/codexclaw/reference/api-mcp/) · [Subagents](/codexclaw/guides/subagents/) |
| Components | <span class="cxc-badge cxc-badge--current">current</span> | 8 component packages: config guard, ops, recall, PABCD state, provider bridge, subagent config, messenger bridge, skill search. GUI is separate. | [How It Works](/codexclaw/concepts/how-it-works/) |
| `cxc` CLI | <span class="cxc-badge cxc-badge--current">current</span> | Payload dispatcher + PATH checkout bin: `enable`, `disable`, `uninstall`, `status`, `orchestrate`, `freeze`, `metric`, `divergence`, `loop`, `goalplan`, `plan`, `scan`, `doctor`, `reset`, `hooks`, `subagents`, `map`, `provider`, `chat`, `memory`, `skill`, `gui`, `serve`, `service`. | [Commands](/codexclaw/reference/commands/) |
| GUI | <span class="cxc-badge cxc-badge--current">current</span> | Dashboard (build from a repo checkout until bundled) plus opt-in loopback messenger bridge through `cxc serve`; not a jaw-style orchestrator. | [GUI Dashboard](/codexclaw/guides/gui/) |

</section>

<section class="cxc-sec">

## Quick start

```bash
# Install from the public marketplace
codex plugin marketplace add lidge-jun/codexclaw
codex plugin add codexclaw@codexclaw

# Verify the marketplace install
codex plugin list
```

The marketplace install activates skills, hooks, MCP, and the payload CLI dispatcher — the
SessionStart banner names the exact invocation when `cxc` is not on `PATH` (see
[Installation](/codexclaw/getting-started/installation/)):

```bash
cxc doctor

# Start a PABCD work-phase from the terminal
cxc orchestrate P --attest '{"from":"IDLE","to":"P","did":"plan the change"}'
```

The project gate is green at 1,631 tests. Build and test commands are documented separately so the
landing page stays a control surface, not a release note.

:::note[Status honesty]
These docs mark features `current`, `planned`, or `deferred`. A `planned` feature has a decided
design but no shipped runtime yet. The docs never describe a planned feature as if it shipped.
:::

</section>

<section class="cxc-sec">

## Where to next

<div class="cxc-index">
  <a class="cxc-index__row" href="/codexclaw/getting-started/installation/">
    <span class="cxc-index__title">Installation</span>
    <span class="cxc-index__desc">Install from the public marketplace or dogfood a source checkout.</span>
    <span class="cxc-index__arrow" aria-hidden="true">-&gt;</span>
  </a>
  <a class="cxc-index__row" href="/codexclaw/concepts/how-it-works/">
    <span class="cxc-index__title">How It Works</span>
    <span class="cxc-index__desc">The plugin boundary: skills, hooks, MCP, file state — no fork, no harness.</span>
    <span class="cxc-index__arrow" aria-hidden="true">-&gt;</span>
  </a>
  <a class="cxc-index__row" href="/codexclaw/guides/pabcd/">
    <span class="cxc-index__title">PABCD Workflow</span>
    <span class="cxc-index__desc">Run the IPABCD loop with attested phase gates and evidence ledgers.</span>
    <span class="cxc-index__arrow" aria-hidden="true">-&gt;</span>
  </a>
  <a class="cxc-index__row" href="/codexclaw/guides/skills/">
    <span class="cxc-index__title">Skills</span>
    <span class="cxc-index__desc">28 skills: the cxc-dev classifier, surface routers, search, recall, QA.</span>
    <span class="cxc-index__arrow" aria-hidden="true">-&gt;</span>
  </a>
  <a class="cxc-index__row" href="/codexclaw/guides/subagents/">
    <span class="cxc-index__title">Subagents</span>
    <span class="cxc-index__desc">Per-role model and prompt config applied to live spawn_agent calls.</span>
    <span class="cxc-index__arrow" aria-hidden="true">-&gt;</span>
  </a>
  <a class="cxc-index__row" href="/codexclaw/development/parity-roadmap/">
    <span class="cxc-index__title">Parity Roadmap</span>
    <span class="cxc-index__desc">What is current, planned, and deferred — status-honest scope.</span>
    <span class="cxc-index__arrow" aria-hidden="true">-&gt;</span>
  </a>
</div>

</section>
